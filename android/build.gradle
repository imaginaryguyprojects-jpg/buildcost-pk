plugins {
    id 'com.android.application' version '8.5.2' apply false
}

allprojects {
    layout.buildDirectory.set(file(System.getProperty("java.io.tmpdir") + "/buildcost-android/" + project.name))
}
