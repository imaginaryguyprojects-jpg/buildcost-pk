{
  "name": "@buildcost/calculations",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@buildcost/config": "workspace:*",
    "@buildcost/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.7"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run --pool=forks"
  }
}
